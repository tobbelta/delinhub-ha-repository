# Delin Hub Home Assistant apps

This repository contains public Home Assistant app metadata for images built
from the private `tobbelta/delinhub` repository.

The source code is kept private. The app image is published separately to
GitHub Container Registry so Home Assistant can update the installed app.
